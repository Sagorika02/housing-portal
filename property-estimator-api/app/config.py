# when running locally, use localhost
# ML_SERVICE_URL = "http://localhost:8000/predict-batch"
# when running with docker compose, the service name is used as the hostname
ML_SERVICE_URL = "http://housing-price-api:8000/predict-batch" 